# stimulus-snippets

Copy-pasteable Stimulus JS controllers for Ruby on Rails. Modelled after shadcn/ui — not an installable package, just files the user owns.

## What this is (and isn't)

- Vanilla JavaScript (no TypeScript, no React, no Vite)
- No build output — the `components/` folder is the deliverable
- No CSS — behaviour only; visual state communicated via `data-*` attributes

## Commands

```sh
yarn format          # Prettier write
yarn format:check    # Prettier check (pre-commit + CI)
yarn lint            # ESLint v9 flat config (pre-commit + CI)
yarn test            # Vitest watch mode
yarn test:run        # Vitest single run (pre-commit + CI)
```

Pre-commit hook runs `yarn format:check && yarn lint && yarn test:run`.  
CI (`.github/workflows/ci.yml`) runs the same three checks on Node 24.

## Project structure

```
components/
  [name]/
    [name]_controller.js        # Stimulus controller (the deliverable)
    [name]_controller.test.js   # Vitest tests
    README.md                   # Usage docs for this component
```

## Adding a component

1. Create `components/[name]/`.
2. Add `[name]_controller.js` — the Stimulus controller.
3. Add `[name]_controller.test.js` — Vitest tests (jsdom environment; use the Stimulus `Application` pattern — see existing tests for the `tick()` / connect setup), including an "accessibility" describe block per `test/axe.js` (see Tooling notes).
4. Add `README.md` following the format in [CONTRIBUTING.md](CONTRIBUTING.md).
5. Add a row to the Components table in the root [README.md](README.md).

## Controller guidelines

- One controller, one job. Split if it does two things.
- Prefer `static targets`, `static values`, `static classes` over `querySelector`.
- Communicate state via `data-*` attributes on elements; let CSS handle visuals.
- Add ARIA attributes in `connect()` only when the HTML author is unlikely to (e.g. `aria-live` on output elements).
- Avoid external dependencies. If `stimulus-use` genuinely saves code, note it as optional.

## Component README format

1. One-sentence description — what it does, not how.
2. Usage — copy the file, register the controller (with code example).
3. HTML — one or more complete, copy-pasteable examples.
4. API — targets, values, and actions in a table.
5. Accessibility notes — ARIA roles, keyboard behaviour, screen reader considerations.

## Tooling notes

- Package manager: Yarn 4.14.1 via Corepack (`yarn@4.14.1` in `package.json`)
- `.yarnrc.yml` — clean: `nodeLinker: node-modules` only (no `enableScripts` / `approvedGitRepositories`)
- Formatter: Prettier with all defaults (`.prettierrc` is `{}`)
- Linter: ESLint v9 flat config (`eslint.config.js`) — `@eslint/js` recommended + `eslint-config-prettier` + `@vitest/eslint-plugin` (test files only); `docs/**` ignored
- Test framework: Vitest 4 + jsdom — `@hotwired/stimulus` is a devDep so controllers can be integration-tested by starting an `Application` in each test suite; scoped to `components/**/*.test.js`
- Accessibility testing: `axe-core` (devDep) run directly against the jsdom container via the shared `test/axe.js` helper (`getA11yViolations(node)`) — not `vitest-axe`, since this repo has no React/RTL to hang that package's matcher off of. Every controller test file has an "accessibility" describe block asserting `getA11yViolations(...)` returns `[]` for the README's documented usage example (and, where relevant, for a second interacted-with state). `color-contrast` and `region` rules are disabled in the helper since they require real CSS layout/full-page landmarks that an isolated jsdom fragment can't provide.
- Pre-commit: Husky — `yarn format:check && yarn lint && yarn test:run`
- CI: `.github/workflows/ci.yml` — `check` job (format + lint + test) + parallel `docs` job (`npm ci && npm run build` in `docs/`)

## Docs site (`docs/`)

Static Astro 5 site deployed to [stimulus-snippets.dev](https://stimulus-snippets.dev).

- **Standalone npm project** — not part of the root Yarn workspace; use `npm` inside `docs/`, not `yarn`
- Content source: `components/*/README.md` via Astro content collection glob loader — no duplicated docs
- A second `guides` content collection (`docs/src/content/guides/*.md`) holds standalone docs not tied to a specific controller (e.g. Rails + Stimulus setup); rendered at `/guides/[slug]` via the same markdown → Shiki pipeline as components, and listed in its own "Guides" sidebar section above "Components"
- Dev server: `http://localhost:3130` (`npm run dev` inside `docs/`)
- Features: sidebar nav, light/dark/system theme toggle (anti-FOUC), GitHub link, Shiki dual-theme code blocks, controller file links rewritten to GitHub blob URLs via remark plugin
- Cloudflare Pages build settings: Root directory `docs`, Build command `npm ci && npm run build`, env var `SKIP_DEPENDENCY_INSTALL=true` (prevents Cloudflare auto-running `yarn install` due to root `yarn.lock`)

## Demo app (`stimulus-snippets-demo`)

A separate sibling repo, [`stimulus-snippets-demo`](https://github.com/craigmcn/stimulus-snippets-demo) (`~/Web/stimulus-snippets-demo`), is a minimal Rails app that demonstrates every shipped controller on a real page in a real browser. It's local-only — not deployed anywhere — and exists purely to manually verify a controller's behavior before/after changes here.

- Once a new component's PR has been reviewed (self-review or otherwise) and any review comments resolved, add a demo for it in `stimulus-snippets-demo` on its own branch + PR, following that repo's `CLAUDE.md` "Adding a demo for a new controller" checklist (copy the controller file, register it in `index.js` with a bare specifier, add it to `DemosController::DEMOS`, add a view adapted from the README's usage example).
- Run it locally at `http://localhost:3140` (`bin/rails server -p 3140`, matching this repo's port table in `~/.claude/active_projects.md`).
- Demo additions don't block merging the component PR in this repo — they're a follow-up step, not a prerequisite.

---

## Progress — 2026-06-19

### Completed

- **11 controllers shipped:** `dismiss`, `clipboard`, `password-reveal`, `character-count`, `password-rules`, `slug`, `checkbox-required`, `tabs`, `accordion`, `form-confirm`, `search-filter`
- **135 tests** across 11 test files — Vitest + jsdom; full Stimulus `Application` integration pattern
- `search-filter` controller built — filters `item` targets against an `input` target's value via substring match on `textContent` (or an opt-in `data-search-filter-term` override), hides non-matches with the native `hidden` attribute, optional `empty` target gets `aria-live="polite"` auto-applied; 11 tests, README, Components table row
- **PR #12 merged** (`feat/search-filter-controller`) — the above; self-reviewed via `/review`, no blocking issues found
- `form-confirm` controller built (`<dialog>`-based replacement for Rails 7's removed `data-confirm`); PR #7 merged, self-reviewed, one real bug found and fixed before merge (see Key decisions)
- Researched and recorded 8 candidate components for future work (see "Candidate components" below), checked against stimulus-components.com, awesome-stimulusjs, and stimulush.com for overlap
- Vitest tooling wired in: `vitest.config.js`, `test`/`test:run` scripts, pre-commit + CI updated
- `@vitest/eslint-plugin` added so `describe`/`it`/`expect`/`vi` globals are recognized in test files
- MIT LICENSE added; `CODEOWNERS` (`* @craigmcn`) added; version bumped to `1.0.0`
- Root `README.md` updated: new components in the table; "Well-covered elsewhere" section listing mature third-party alternatives
- Initial commit pushed to `main` on `github.com/craigmcn/stimulus-snippets`
- Branch ruleset applied to `main` via `gh` CLI (1 required review, stale-review dismissal, `check` status check, no force-push/delete)
- **PR #1 merged** — bug fixes: `clipboard` navigator guard + timer debounce + disconnect; `checkbox-required` pristine-form init; `password-rules` RegExp guard
- **PR #2 merged** — docs site in `docs/` (Astro 5, Cloudflare Pages, stimulus-snippets.dev); dark/light/system theme toggle; CI docs job
- **PR #3 merged** (`fix/follow-up-cleanups`) — `slug` lock DOM-backed via `lockedValue`; `slug` README non-Latin note; `checkbox-required` `console.warn` when outside form; `console` + `KeyboardEvent` added to ESLint globals
- **PR #4 merged** (`feat/tabs-controller`) — `tabs` controller: full ARIA tablist, arrow-key nav, auto ID generation; addressed code review feedback from Claude and Copilot; 90 tests total
- **PR #5 merged** (`feat/accordion-controller`) — `accordion` controller: ARIA disclosure pattern, arrow-key nav, exclusive-open mode; `_closePanel(index)` helper extracted per Copilot review; 115 tests total
- **PR #6 merged** (`feat/seo-discoverability`) — SEO and AI discoverability improvements to `docs/`
- DNS/email spoofing records (`SPF`, `DMARC`, `www` redirect) resolved in Cloudflare
- Docs site accent color changed from blue (`#2563eb`) to mustard/ochre (`#b45309` light / `#d29922` dark) — trialed against deep maroon/burgundy side-by-side in the running dev server before deciding
- Mustard accent extended beyond links/active nav to: wordmark (`.logo-primary`), active sidebar nav (inset accent stripe via `box-shadow`), component-card hover border, and `prose h2` underline (now 2px, accent-colored)
- `docs/public/favicon.svg` background updated from blue to mustard (`#b45309`) to match
- **PR #8 merged** (`feat/mustard-accent`) — the above mustard accent + favicon changes
- New "Setting up Rails with Stimulus" guide added (`docs/src/content/guides/getting-started.md`) — covers new vs. existing Rails apps, importmap vs. `jsbundling-rails` install paths, verifying the install, copying/registering a controller, smoke-testing, and common gotchas; new `guides` content collection + `/guides/[slug]` route + sidebar nav section; linked from root README's "How to use"
- Fixed `<pre>` code blocks blending into the page background in light mode — Shiki's light theme background (`#fff`) matched the page background exactly; forced a light-gray background (`var(--inline-code-bg)`) and added a subtle border in both themes for definition
- **PR #9 merged** (`docs/rails-setup-guide`) — the above guide + light-mode contrast fix
- Full-codebase review from a senior-Rails-developer perspective (all 10 controllers + docs): found one real bug (`form-confirm` submitter loss, see Key decisions) and two getting-started-guide gaps (layout script-tag check, CSP/importmap nonce gotcha) — both doc gaps folded into the same fix
- **PR #10 merged** (`fix/form-confirm-submitter`) — submitter-preservation fix + new multi-submit-button test (124 tests total) + the two guide additions above
- Docs site mobile/tablet responsive review: sidebar nav now collapses behind a hamburger toggle (`.sidebar-toggle`, `aria-expanded`) below 768px instead of stacking fully above content; added `nav + nav` margin so Guides/Components/All sections have visible separation; GitHub link + theme toggle shrunk and laid out as a compact row on mobile instead of full-width blocks; `.prose th { white-space: normal }` on mobile to stop API table headers from forcing horizontal page scroll
- **PR #13 opened** (`fix/mobile-docs-nav`) — the above mobile nav/table fixes
- `file-preview` controller built — previews selected files before form submit: image thumbnails via object URLs (revoked on re-render/disconnect), filename/size for everything else; `clear` action resets the whole selection; no per-file remove (`DataTransfer` FileList rebuilding is real-browser-only, no jsdom equivalent); 13 tests, README, Components table row
- **PR #14 merged** (`feat/file-preview-controller`) — the above; Copilot review caught two real issues fixed before merge: filename/size spans ran together with no separator in unstyled markup, and object-URL revocation wasn't covered by tests (see Key decisions); 148 tests total
- `dependent-select` controller built — filters one `<select>`'s options based on another select's value (e.g. Country → State) via `data-dependent-select-group` on options; non-matching options are hidden+disabled, stale dependent selections are cleared on group change; 8 tests, README, Components table row
- **PR #15 merged** (`feat/dependent-select-controller`) — the above; reviewed via the new `copilot-review-triage` skill (see Key decisions): Copilot's one comment was valid (PR description claimed 12 tests, actual was 8 — fixed the description, no code change needed); also documented an undocumented edge case (`data-dependent-select-group=""` matches an empty group selection, unlike omitting the attribute) found during my own independent pass; 156 tests total
- Built a new global skill, `~/.claude/skills/copilot-review-triage/SKILL.md` — fetches a PR's Copilot review comments via `gh api .../pulls/{n}/comments` + `/reviews`, requires verifying each against the actual file/diff (not taking Copilot's framing at face value), runs an independent review pass on top, consolidates both into one severity-tagged list, drafts a plan, and **always stops for explicit confirmation before implementing — even in auto mode** (the user's explicit requirement)
- New docs guide added: `docs/src/content/guides/well-covered-elsewhere.md` — surfaces the root README's "Well-covered elsewhere" table (third-party Stimulus component packages) plus the `field-sizing: content` CSS-only note and a new `stimulus-use` recommendation; previously this list existed only in the README, not on the docs site. Wired into the sidebar's "Guides" nav in `Layout.astro`; build verified (`npm run build`) and the new `/guides/well-covered-elsewhere` page renders with correct title/description
- PR #16 self-review (via `/review`) flagged a drift risk: the table now existed in both `README.md` and the new guide with no link between them, so a future edit to one could silently fall out of sync with the other. Fixed by making the guide the single source of truth: `README.md`'s "Well-covered elsewhere" section is now a one-line summary + link to `https://stimulus-snippets.dev/guides/well-covered-elsewhere`, table and `field-sizing` note removed from README (see Key decisions)
- **PR #16 merged** (`docs/well-covered-elsewhere`) — the above guide + README link-out fix; `main` fast-forwarded locally
- `datetime-local` controller built — formats a server-rendered UTC timestamp (read from a `<time datetime>` attribute, falling back to text content) into the viewer's local time zone via `Intl.DateTimeFormat`; `dateStyle`/`timeStyle` values (each settable to `none`) control which parts render, plus optional `locale`/`timeZone` overrides; the `datetime` attribute itself is left untouched so the element stays machine-readable; unparseable source values leave the element as-is; 6 tests, README, Components table row
- Triaged via `copilot-review-triage` on PR #17: Copilot's review caught a real gap (`Intl.DateTimeFormat` construction had no try/catch, so an invalid `data-*` `timeZone`/`dateStyle`/`timeStyle` would crash the controller — inconsistent with `password-rules`' own RegExp try/catch precedent) and a flakiness risk (tests hardcoded `Intl.DateTimeFormat` output strings instead of deriving them, which can drift across Node/ICU versions since the repo had no pinned Node version at the time); both fixed, plus a regression test for the invalid-`timeZone` case and two added coverage-gap tests (both styles `none`; no source at all); 9 tests total for this controller
- Node version pinned repo-wide in response to the above flakiness finding: added `.node-version` (`24`) and `engines: { node: ">=24" }` in both `package.json` and `docs/package.json`; `ci.yml` now reads `node-version-file: .node-version` in both jobs instead of hardcoding `node-version: 24` twice
- Documented the `stimulus-snippets-demo` sibling repo in this `CLAUDE.md` (local-only Rails app for viewing components in action; demo additions are a post-review follow-up, not a merge prerequisite)
- **PR #17 merged** (`feat/datetime-local-controller`) — all of the above (controller, Copilot-triage fixes, Node pinning, demo-app docs note); `main` fast-forwarded locally; 165 tests total
- `datetime-local` demo added to the sibling `stimulus-snippets-demo` repo (controller copy, `index.js` registration, `DemosController::DEMOS` entry, view with default/date-only/time-only-long variants); verified live via a real-browser screenshot — correctly converts the server's UTC timestamp to the browser's local time, no console errors, no horizontal scroll
- **PR #5 merged** in `stimulus-snippets-demo` (`feat/datetime-local-demo`) — the above demo; `main` fast-forwarded locally in that repo
- `number-format` controller built — live thousands-separator grouping while typing (cursor position preserved by digit count, not character offset), full `Intl.NumberFormat` styling (`decimal`/`currency`, `locale`, `minimumFractionDigits`/`maximumFractionDigits`) applied on blur, optional `hidden` target carries a clean unformatted numeric string for form submission; `locale` deliberately affects only the formatted display, never parsing (typed/submitted values always use `.` as the decimal separator — round-tripping a locale's own separators is ambiguous); 11 tests, README, Components table row
- Triaged via `copilot-review-triage` on PR #19: Copilot caught two real bugs — `focus()` stripped formatting from the display but never synced the `hidden` target (left it stale), and the caret jumped _before_ a lone leading `-` while typing a negative number, so the next keystroke landed ahead of the sign and `_sanitize` silently dropped it. Also caught a misleading test title (claimed "no currency code" but the fixture supplied an invalid one). My own independent pass on top found `connect()` would unconditionally reformat even if the input was already focused (e.g. a Turbo morph reconnect mid-edit), clobbering in-progress typing. All four fixed; two cosmetic/optional findings (leading-decimal hidden/display string mismatch, missing default-currency-fallback test) were deliberately left out as not worth the added complexity; 178 tests total
- **PR #19 merged** (`feat/number-format-controller`) — all of the above; `main` fast-forwarded locally
- Automated accessibility testing wired in across all 15 existing controllers: added `axe-core` devDep + a shared `test/axe.js` helper (`getA11yViolations(node)`, with `color-contrast`/`region` rules disabled since jsdom has no real layout engine and these fixtures are isolated component snippets, not full pages); every controller test file gained an "accessibility" describe block asserting zero violations against the README's documented usage example, with a second state-after-interaction test for controllers whose ARIA/DOM changes meaningfully on toggle (`accordion`-style open/closed, `tabs` tab switch, `password-reveal` reveal, `checkbox-required`/`search-filter` error/empty-state shown, `form-confirm` dialog open, `file-preview` after files selected, `dependent-select` after a group is chosen). Caught one real, fixable doc gap in the process: `clipboard`'s README example had a readonly `source` input with no accessible name — fixed by adding `aria-label` to both README examples and a new bullet in the Accessibility section. 202 tests total (up from 178).
- `README.md`'s "Accessible" philosophy bullet and `CONTRIBUTING.md`/this file's "Adding a component" steps updated to require the new accessibility test block for all future controllers.
- **PR #21 merged** (`feat/axe-a11y-testing`) — the above; self-review caught two consistency nits (fixed: `clipboard`'s duplicated fixture HTML hoisted into a shared `A11Y_HTML` const; `form-confirm`'s accessibility checks rescoped from `document.body` to the `widget` element, matching the other 13 controllers); triaged via `copilot-review-triage` — Copilot's one comment was valid — `test/axe.js` imported axe-core's named `{ run }` export, but axe-core ships pure CJS with no `exports` map (`"main": "axe.js"` only), so `run` only resolved because Vitest/Vite's runtime CJS interop happens to proxy every property as a named export; verified directly that plain Node ESM resolves `run` to `undefined` (and an unrelated grab-bag of internal symbols) while `import axe from "axe-core"; axe.run(...)` works in both. Fixed; `main` fast-forwarded locally; 202 tests total.
- `table-sort` controller built — click a `<th>`'s `button` to sort `<tbody>` rows by that column; type (string/number/date) auto-detected from the first row's cell with `data-table-sort-type` override and `data-table-sort-value` per-cell override for display-vs-sort-key mismatches; `aria-sort` set on the `<th>` and kept in sync as columns are sorted; column index read from the native `th.cellIndex` (not target array position) so non-sortable columns can be interleaved; 9 tests, README, Components table row; 211 tests total
- Triaged via `copilot-review-triage` on PR #22: Copilot caught a real bug — `data-table-sort-type` was taken verbatim with no validation, so a typo'd or wrongly-cased value (e.g. `"strnig"`) skipped auto-detection entirely _and_ failed the comparator's `type === "string"` check, falling into the numeric branch and producing `NaN` comparisons on non-numeric text — the table looked unsorted with no error. Fixed by validating the override against a `VALID_TYPES` allow-list and falling back to `_detectType` otherwise. Copilot's other comment (CLAUDE.md test-count math) was already fixed before triage. My own independent pass added the missing `date`-type test coverage (auto-detected and explicit `_valueFor`/comparator branches had zero direct test coverage before this). 213 tests total.
- **PR #22 merged** (`feat/table-sort-controller`) — `main` fast-forwarded locally.
- `row-select` controller built — per-row checkboxes plus a `select-all` checkbox with checked/indeterminate sync, shift-click range select, and a bulk-actions bar (`bar`/`count` targets) shown once any row is selected; selected rows get `data-row-select-selected`; `count` gets `aria-live="polite"` auto-applied; 13 tests, README, Components table row
- Copilot review on PR #23 flagged 3 comments: one valid (`toggle` should use `event.currentTarget`, not `event.target`, matching `table-sort`/`tabs`'s convention — fixed, with an `index === -1` guard and a regression test), two invalid (claimed `dispatchEvent(new MouseEvent("click", ...))` doesn't trigger jsdom's default checkbox-toggle behavior — refuted by a direct probe: jsdom _does_ run the default action for dispatched `click` events on `<input type="checkbox">`, same "verify before trusting Copilot's framing" pattern as the axe-core CJS finding on PR #21).
- `/review-pr` (independent of Copilot) on PR #23 found a second, different real bug: `_lastIndex` (the shift-click range anchor) was never re-validated against the current `rowTargets.length`, so removing a row from the DOM without the controller reconnecting (e.g. a Turbo Stream `remove` after using the bulk-actions bar's own "delete selected" affordance — the exact use case this controller exists for) left a stale index that could point past the shrunk array, crashing the next shift-click's range loop with `TypeError: Cannot set properties of undefined`. Fixed by skipping range-select when the stored anchor is out of bounds, falling back to a plain single-row toggle; added a regression test simulating a row removed mid-session. 15 tests for this controller, 228 total.
- Recorded four new next-component candidates in this file's "Next components" list (`relative-time`, `match-validator`, `direct-upload-progress`, `multi-step-form`) and extended the docs' `well-covered-elsewhere` guide with four more mature third-party packages (modal, drag-and-drop reorder, hotkeys, async content loading) found while cross-checking for overlap
- **PR #23 merged** (`feat/row-select-controller`) — `main` fast-forwarded locally.
- `row-select` demo added to the sibling `stimulus-snippets-demo` repo (controller copy, `index.js` registration, `DemosController::DEMOS` entry, a 5-row table view whose "Delete selected" button removes checked rows directly from the page — no DB in that app to persist against); verified live via Playwright against the system Chrome: range select, delete, and a post-delete shift-click (deliberately exercising the stale-range-anchor fix above) all behaved correctly with zero console errors.
- **PR #8 merged** in `stimulus-snippets-demo` (`feat/row-select-demo`) — the above; `main` fast-forwarded locally in that repo.
- `unsaved-changes` controller built — marks a `<form>` dirty on any `input`/`change` inside it and clears the flag on `submit`, no per-field `data-action` wiring required; guards leaving the page two ways: the native `beforeunload` dialog for full page unloads, and a `window.confirm()` prompt (with a `messageValue`-controlled message) on `turbo:before-visit` for Turbo Drive navigations; `markDirty`/`markClean` exposed as public actions for manual use (rich-text editors with non-standard change events, Turbo Stream saves with no real `submit` event); 11 tests, README, Components table row; 239 tests total
- Added `window` to the ESLint flat config's global allow-list (`eslint.config.js`) — first controller to reference it directly; previously only `navigator`/`document` and other DOM globals were declared.
- `relative-time` controller built — formats a server-rendered timestamp (read from a `<time datetime>` attribute, falling back to text content, same pattern as `datetime-local`) into a live "5 minutes ago"/"in 2 days" style string via `Intl.RelativeTimeFormat` and the standard MDN time-ago division table (seconds → minutes → hours → days → weeks → months → years); re-renders on a `setInterval` (default 60s, overridable via `intervalValue` for finer-grained live feeds) and clears it on `disconnect`; `aria-live="polite"` auto-applied on connect; `styleValue`/`numericValue` map directly to `Intl.RelativeTimeFormat` options, with a try/catch guard around formatter construction matching the `datetime-local`/`password-rules` precedent for invalid `data-*` overrides; 12 tests, README, Components table row; 253 tests total
- Triaged via `copilot-review-triage` on PR #25: Copilot caught two real bugs and one false positive. Real: (1) `connect()` unconditionally overwrote `aria-live`, ignoring an author-provided value — inconsistent with `character-count`/`row-select`/`search-filter`'s `!hasAttribute("aria-live")` guard, now matched here too. (2) When falling back to `textContent` as the source (no `datetime` attribute), `render()` was overwriting `textContent` with its own formatted output, so the _next_ `setInterval` tick tried to re-parse the rendered string (e.g. `"5 minutes ago"`) as a date, failed, and the display froze forever — fixed by caching the original source string once in `connect()` and reusing it on every `render()` call instead of re-reading `textContent`. False positive: Copilot claimed passing plural unit strings ("seconds" vs "second") to `Intl.RelativeTimeFormat#format` throws `RangeError` — refuted directly in Node (`rtf.format(-5, "seconds")` → `"5 seconds ago"`, no error); ECMA-402 normalizes plural unit strings, no code change needed. My own independent pass found a third, distinct bug Copilot missed: the unit-selection check compared the _unrounded_ duration against each division's threshold but formatted `Math.round(duration)`, so a duration like -59.6s passed the `< 60`-seconds check and then rounded to -60, producing "60 seconds ago" instead of "1 minute ago" (and the equivalent at the 60-minute boundary) — the exact rounding case the original commit message incorrectly claimed the algorithm avoided. Fixed by rounding once per loop iteration and using that rounded value for both the threshold comparison and the formatted output. Added regression tests for all three; 15 tests total for this controller, 256 overall.
- Added `setInterval`/`clearInterval` to the ESLint flat config's global allow-list — first controller to use them (previous timer-based controllers like `clipboard`/`form-confirm` only needed `setTimeout`/`clearTimeout`).
- `match-validator` controller built — requires two fields (`field`/`match` targets) to hold the same value before form submit, the generic "type it again" pattern behind password/email confirmation; follows the `checkbox-required` template (submit-time `preventDefault`, `data-valid` on the wrapping element, optional `error` target, `console.warn` when outside a form). Adds `aria-invalid` on the `match` target on every `validate` call (first controller to set `aria-invalid` rather than relying solely on `data-valid`/`hidden`). Optional `caseSensitive` value (default `true`) lowercases both sides before comparing for email-confirmation-style use cases. Empty/empty counts as a match by design — consistent with the project's no-premature-complexity stance; pairing with HTML5 `required` (or `checkbox-required` for groups) covers the emptiness case separately. 16 tests, README, Components table row; 272 tests total.
- `direct-upload-progress` controller built — listens for the `direct-upload:*` custom events Rails ActiveStorage's own direct-upload JS already dispatches on the file input (`initialize`/`progress`/`error`/`end`) and renders a per-file `<li>` with a native `<progress>` element; no upload logic of its own, purely a UI layer over ActiveStorage's existing wire format. `data-direct-upload-progress-state` (`uploading`/`done`/`error`) set per item for styling hooks. Optional `status` target gets `aria-live="polite"` auto-applied (matching the `search-filter`/`relative-time` precedent) and is updated only on add/success/failure, not on every progress percentage tick, to avoid spamming screen reader users. 14 tests, README, Components table row; 286 tests total.
- Triaged via `copilot-review-triage` on PR #27: Copilot caught two real issues, both stemming from the same root cause — `error()`/`end()` never removed an upload's entry from the internal `_items` Map once it reached a terminal state, so a long-lived page (Turbo-cached, or a form reused across many selections without reload) would leak one Map entry per finished upload indefinitely. My own independent pass found the same gap had a correctness angle beyond the memory leak Copilot named: because the entry stayed live, a late/out-of-order `direct-upload:progress` event for an already-finished or already-failed id would still hit `progress()`'s lookup and overwrite the progress bar's value, visually un-sticking a bar that should stay frozen at its terminal state. One fix (`this._items.delete(id)` at the end of both `error()` and `end()`) closes both the leak and the stray-event bug at once; `progress()`'s existing `if (!item) return` guard now also covers "finished" the same way it already covered "unknown". Copilot's third comment (the filename `<span>`'s `data-direct-upload-progress-role="name"` was undocumented, unlike `file-preview`'s equivalent hook) was also valid and fixed in the README. Two new regression tests assert a late `progress` event after `error`/`end` no longer changes the displayed value; 16 tests for this controller, 288 overall.
- `number-format` (previously #1 on the "Next components" list) shipped via PR #19. `table-sort` (previously #1) shipped via PR #22. `row-select` (previously #1) shipped via PR #23. `unsaved-changes` (previously #1) shipped via the PR documented above. `relative-time` (previously #1) shipped via the PR documented above. `match-validator` (previously #1) shipped via the PR documented above. `direct-upload-progress` (previously #1) shipped via the PR documented above. `multi-step-form` was the only item left on that list; see "Next components" below for why it's being shelved rather than built.
- **PR #13**, **PR #16**, **PR #17**, **PR #19**, **PR #21**, **PR #22**, **PR #23** (`feat/row-select-controller`), **PR #25** (`feat/relative-time-controller`), **PR #26** (`feat/match-validator-controller`), and **PR #27** (`feat/direct-upload-progress-controller`) in this repo all merged; **PR #5** (`feat/datetime-local-demo`), **PR #8** (`feat/row-select-demo`), **PR #11** (`feat/relative-time-demo`), and **PR #12** (`feat/match-validator-demo`) in `stimulus-snippets-demo` also merged. The `row-select` demo (5-row table; "Delete selected" removes checked rows directly from the page) was verified live via Playwright against the system Chrome — range select, delete, and a post-delete shift-click (exercising the stale-range-anchor fix from PR #23) all behaved correctly with zero console errors. The `relative-time` demo (four `<time>` elements covering past/future timestamps, the `numeric: "always"` override, and a custom render interval) was likewise verified live via Playwright with zero console errors. The `match-validator` demo (password/confirm-password and case-insensitive email/confirm-email examples) was likewise verified live via Playwright with zero console errors.

### Key decisions

- **`_initValid()` vs. calling `validate()` from connect** — `validate()` has two jobs (set `data-valid` + toggle error target). Calling it raw from `connect()` showed errors before user interaction. Extracted `_initValid()` to do only the data-attribute write; `validate()` continues to manage error visibility on user events.
- **Blanket `.catch(() => {})` retained** — the `.then()` body contains no realistic throw paths, so a narrower catch would add complexity for no benefit. The guard before the call handles the only real crash path (undefined clipboard).
- **Docs site: monorepo in `docs/`** — Astro reads `components/*/README.md` directly; no duplicated content, no sync mechanism. Standalone npm project (not Yarn workspace) to allow `npm ci` on Cloudflare Pages.
- **Cloudflare Pages workflow** — must use Pages (not Worker) project type. Custom domains added through Pages dashboard only, not via manual DNS. `SKIP_DEPENDENCY_INSTALL=true` required because root `yarn.lock` causes Cloudflare to auto-run `yarn install` in `docs/`.
- **`slug` locked state uses `lockedValue`** — backed by `data-slug-locked-value` attribute so it survives controller reconnects (e.g. Turbo morphing). `connect()` only sets it when not already locked, so a reconnect with an empty output field doesn't un-lock a manually edited slug.
- **`checkbox-required` warns when outside a form** — `console.warn` on connect when `closest("form")` returns null; submit validation silently did nothing before.
- **`tabs` uses `_activate()` directly from action methods** — Stimulus value callbacks fire via MutationObserver (async); calling `_activate()` directly from `select()` and `keydown()` keeps UI updates synchronous and keeps tests simple. `indexValueChanged` is kept but guarded by `this._activated` so it only fires for external programmatic changes after connect.
- **`tabs` ID auto-generation uses module-level `uid` counter with collision check** — `do...while (document.getElementById(id))` skips any candidate already in the document; avoids `crypto.randomUUID()` churn on reconnect. On Vite HMR the counter resets but this is a development-only edge case.
- **`tabs` `_activate` clamps index** — out-of-bounds `data-tabs-index-value` (or programmatic set) is clamped to `[0, count-1]`; guard for empty tab list. Prevents the silent broken state where all panels hide and no tab is reachable.
- **`accordion` uses heading + button pattern, not `<details>`/`<summary>`** — `<details name>` covers single-open natively in 2026, but the ARIA accordion pattern (heading + `aria-expanded`) gives screen reader users heading-level navigation. Controller adds value where `<details>` can't: `aria-controls`/`aria-labelledby` wiring, arrow-key focus movement between triggers, and coordinated exclusive-open mode. `toggle()` called directly from action methods (not via value callback) so panel state updates are synchronous.
- **`accordion` exclusive enforcement runs on both connect and toggle** — if the page loads with multiple panels open and `exclusive` is true, all but the first open panel are closed on connect. This normalizes authoring mistakes without requiring JS-only initialization.
- **Docs SEO/discoverability: `@astrojs/sitemap` + OG/JSON-LD + `/llms.txt` + `/all`** — sitemap auto-generated at build; OG/Twitter/JSON-LD (`WebSite` on home, `TechArticle` on controller pages) in `Layout.astro`; `/llms.txt` is a dynamic Astro endpoint (auto-updates as controllers are added); `/all` renders all READMEs in one page for LLM single-request reads. JSON-LD uses `Astro.site?.origin` (not a hardcoded string) and escapes `<` as `<` to prevent `</script>` breakout. Distribution tactics (community links, GitHub Topics) intentionally deferred.

- **`form-confirm` uses a single `_allowed` re-entry guard, not a separate "real" event** — `intercept()` preventDefault's and opens the dialog; `proceed()` sets the guard then calls `requestSubmit()`/`click()` on the original source element, so the same listener sees the replay and lets it through. Works for both form `submit` and link/button `click` without Turbo-specific code. Documented limitation: Turbo's `data-turbo-method` links bypass native form submission entirely, so they need `Turbo.setConfirmMethod()` instead — out of scope for a vanilla Stimulus controller.
- **`form-confirm` guards against double `showModal()` calls** — calling `showModal()` on an already-open `<dialog>` throws `InvalidStateError` in real browsers; this path is invisible to the test suite since jsdom doesn't implement `showModal` at all (confirmed by direct probe, not just absence in test output). `_open()` now checks `this.dialogTarget.open` first. Found via self-review of PR #7 before merge, not via test failure — worth remembering that this controller's dialog-open path is structurally undertested in jsdom and needs reasoning about real-browser behavior directly.
- **`form-confirm` preserves the clicked submit button across resubmit** — `requestSubmit()` with no argument resubmits via the form's default submitter, silently dropping which button was clicked. Broke the common Rails multi-submit-button pattern (`form.button "Save"` / `form.button "Save and add another"` sharing a `commit` name) — wrong value would reach the server after confirming. Fixed by capturing `event.submitter` in `intercept()` and passing it to `requestSubmit(submitter)` in `proceed()`. Found via a full-codebase senior-Rails-dev review (PR #10), not a test failure — same class of "real browser behavior, not exercised by the existing test fixtures" gap as the `showModal()` bug above.
- **Docs accent: mustard over maroon** — both were prototyped live in the dev server (swapping CSS variable values) before picking; mustard read as more distinctive than blue without the "alarm/error" connotation maroon risked. Decided via direct visual comparison, not by description alone.
- **`search-filter` plural-target iteration, no debounce** — `itemTargets` (plural) is always a safe empty-array default, so `filter()` needs no `hasItemTarget` guard before iterating; only the singular `inputTarget` access is guarded. No debounce added on the `input` action — full re-filter on every keystroke is cheap for typical Rails-page list sizes; flagged in review as a known scaling boundary, not a defect, consistent with the project's no-premature-complexity stance.
- **`file-preview` separates name/size with a literal text node, not CSS** — Copilot review on PR #14 caught that the generated `name`/`size` `<span>`s were appended with no whitespace between them, so default (unstyled) rendering ran them together (`"a.txt2.0 KB"`) — a real bug, since the README explicitly says no CSS is assumed. Fixed with a `" · "` text node between the spans rather than relying on a consumer's CSS.
- **`file-preview` object-URL revocation needed its own test** — the controller's most important resource-management behavior (revoking previous object URLs on re-render and on disconnect) had no assertion in the original test suite; same review caught this. Added `vi.spyOn(URL, "revokeObjectURL")` tests covering re-render, disconnect, and the no-image-files case — `URL.createObjectURL`/`revokeObjectURL` are real, spyable functions in jsdom (unlike `DataTransfer`, which jsdom doesn't implement at all), so this didn't hit the same real-vs-jsdom wall as the `form-confirm`/mobile-nav cases above.
- **Solved the headless-Chromium sandbox blocker — `channel: 'chrome'` instead of Playwright's bundled download.** Playwright's own Chromium download carries the macOS quarantine flag and fails to launch from a sandboxed shell (`spawn ... -88`); stripping quarantine is a denied security-weakening action. Launching against the already-Gatekeeper-cleared system Chrome (`chromium.launch({ channel: 'chrome' })`) sidesteps it entirely, no permission changes needed. Built a reusable global helper at `~/.claude/scripts/playwright-chrome/screenshot.js` (generic: takes URL/width/height/outfile/`--click`/`--full-page`, prints `horizontalScroll` + console errors as JSON) so this works across all projects, not just this one. Documented as a project skill at `docs/.claude/skills/screenshot-mobile/SKILL.md` so future sessions in this repo find it automatically via the `run` skill's project-skill lookup.
- **`dependent-select` uses `hidden` + `disabled` together on non-matching options, not CSS** — matches the project's "no CSS assumed" stance; `disabled` alone isn't enough because some browsers still show a disabled option in the closed `<select>` if it happens to be the current value, and `hidden` alone doesn't block keyboard typeahead from landing on it.
- **`dependent-select` resets the dependent value (not the group value) when a selection no longer matches** — resetting the group instead would be surprising (the user didn't touch that field) and would cascade confusingly if more than one dependent select existed.
- **Built `copilot-review-triage` as a global skill (`~/.claude/skills/`), not a project skill** — it has no dependency on this repo's tooling (just `gh` CLI), so it belongs at the user level where every repo can use it, unlike `screenshot-mobile` which is genuinely specific to this repo's docs site.
- **First mobile-nav fix attempt was verified only by static analysis and shipped a wrong root-cause guess** — without a real screenshot, guessed the horizontal-scroll cause was API-table `<th>` `white-space: nowrap` and added `white-space: normal` on mobile. Once the screenshot tooling above was built and used for real, this turned out to be wrong: actual cause was an unbreakable inline `<code>` token (e.g. `successDuration`) in a table _body_ cell forcing the table past viewport width — headers were never the problem. Real fix: `.prose table { display: block; overflow-x: auto; }` (mirrors the existing `.prose pre` overflow pattern), and the `th` mobile override was removed as ineffective. **Lesson: static CSS analysis caught the symptom category but guessed the wrong element; a real rendered screenshot found the actual offending node in one pass** — worth always reaching for the real browser check before describing a CSS-overflow fix as done.
- **"Well-covered elsewhere" guide is a new standalone guide page, not folded into an existing one** — it's conceptually distinct from "Setting up Rails with Stimulus" (an onboarding walkthrough vs. a reference list of third-party alternatives), so it gets its own slug and sidebar entry rather than a section bolted onto the getting-started guide.
- **Sidebar "Guides" nav remains hand-written per-entry in `Layout.astro`, not generated from the `guides` collection** — consistent with the existing pattern (the `getting-started` link was already hardcoded); a loop over `getCollection('guides')` would be a reasonable future cleanup if a third guide gets added, but two manual entries doesn't yet justify the refactor.
- **README links to the docs guide instead of duplicating its table** — caught in self-review of PR #16: having the same "well-covered elsewhere" table live in both `README.md` and the docs guide meant any future addition to the list had to be remembered in two places with no enforcement. Rather than building a shared-data-source mechanism (e.g. a JSON/YAML file both README and Astro read) for a single seven-row table, simplest fix was to make the docs guide canonical and have the README link out to it — mirrors how GitHub READMEs commonly point to a hosted docs site for anything beyond a quick orientation.
- **`number-format` keeps `hidden` as the exact raw sanitized string, never a re-stringified `Number`** — avoids float round-tripping/precision loss entirely (e.g. no risk of `12.50` → `12.5` → reformat surprises on large values); the displayed value can be lossy via `Intl` formatting, but the submitted value never is.
- **`number-format` `connect()` skips reformatting if the input is already the active element** — guards against a Turbo morph reconnecting the controller mid-edit and overwriting in-progress typing with the full currency-formatted string. Same class of "real browser/Turbo timing" gap as `slug`'s lock-on-reconnect guard and `accordion`'s connect-time exclusive enforcement.
- **a11y tests run against README-derived fixtures, not the existing minimal functional-test fixtures** — most controllers' functional tests intentionally use bare-bones HTML (no `<label>`, no wrapping `<form>`) to keep assertions focused; running axe against those as-is would flag "missing label" on fixtures that were never meant to be accessible examples, not real controller bugs. Each accessibility describe block instead reconstructs the README's documented (accessible) usage HTML, so the assertion is "the recommended way to use this controller is accessible" rather than "every test fixture in the file is accessible."
- **`color-contrast` and `region` axe rules disabled in `test/axe.js`** — jsdom doesn't run a real layout/paint engine, so `color-contrast` can't reliably evaluate computed styles, and `region` expects full-page landmark structure (`<main>`, etc.) that a mounted component fragment will never have. Both are real checks worth doing in a real browser (e.g. via the `stimulus-snippets-demo` sibling repo or a Lighthouse/axe browser extension pass) but produce false positives/negatives in this test environment.
- **`test/axe.js` imports axe-core's default export, not a named `{ run }` import** — axe-core has no ESM `exports` map, only `"main": "axe.js"` (pure CJS). A named import only worked by accident, because Vitest/Vite's runtime CJS interop proxies every property of `module.exports` as a named export; plain Node ESM resolves `run` to `undefined` instead. `import axe from "axe-core"` + `axe.run(...)` is portable across both and has no downside. Caught by Copilot review on PR #21, verified directly via a standalone `node -e "import('axe-core')..."` probe rather than taken at face value.
- **`table-sort` reads the column index from `th.cellIndex`, not from the header target's position in `headerTargets`** — `headerTargets` only contains _sortable_ columns, so if a non-sortable column (e.g. an actions column with no `header` target) sits between sortable ones, an index derived from array position would point at the wrong `<td>`. The native `cellIndex` property always reflects the real position in the `<tr>`, so non-sortable columns can be mixed in safely.
- **`table-sort` puts `aria-sort` on the `<th>`, not the `<button>` inside it** — matches the ARIA Authoring Practices columnheader pattern; the button exists only to get free keyboard support (Tab/Enter/Space) without the controller needing its own keydown handler, same minimal-code instinct as `accordion`/`tabs`'s native-element-first approach.
- **`table-sort` type auto-detection priority is number → date → string** — checked only against the first row's cell. A 4-digit value like a year or ID is ambiguous (valid as both a number and a date), so number wins by priority; `data-table-sort-type` is documented as the escape hatch for columns where the first row isn't representative or detection picks the wrong type.
- **`table-sort` validates `data-table-sort-type` against a `VALID_TYPES` allow-list instead of trusting it verbatim** — an unvalidated typo'd/wrongly-cased value (e.g. `"strnig"`) would fail both `_valueFor`'s `"number"`/`"date"` checks _and_ `sort()`'s `type === "string"` check, landing in the numeric comparator branch with string operands and producing `NaN` comparisons — an unsorted table with no visible error. Falling back to `_detectType` on any unrecognized value makes a typo degrade to "ignored" rather than "silently broken." Caught by Copilot review on PR #22.
- **`row-select` listens on `click`, not `change`, for row checkboxes** — `change` events on checkboxes are plain `Event`s with no modifier-key info; `click` is a `MouseEvent` and carries `shiftKey`, which the range-select feature needs. By the time the `click` listener runs, the browser's default action has already toggled `checked`, so `currentTarget.checked` reliably reflects the new state inside the handler — confirmed directly against jsdom (it implements this default action for dispatched events too, not just real `.click()` calls).
- **`row-select`'s shift-click range anchor (`_lastIndex`) is clamped against `rowTargets.length`, not trusted across DOM mutations** — found via independent review (not Copilot), distinct from the `event.target`→`currentTarget` fix Copilot caught on the same PR. A row removed from the DOM (e.g. a Turbo Stream `remove` after the controller's own bulk-delete action) leaves a stale stored index with no controller reconnect to reset it; an out-of-bounds range access would throw. Skipping range-select when the anchor is now out of bounds degrades gracefully to a single-row toggle instead of crashing.
- **`row-select`'s `data-controller` must wrap both the `<table>` and the bulk-actions bar, not just the table** — Stimulus only resolves targets that are descendants of the controller's own element; an early draft put `data-controller` on the `<table>` itself with the bar as a sibling, silently breaking every `bar`/`count`/`all` target lookup with no error (the table genuinely had no matching descendants). README's HTML example and all tests wrap both in a shared outer `<div data-controller="row-select">`.
- **`unsaved-changes` listens on its own element for `input`/`change`/`submit`, but on `window`/`document` for `beforeunload`/`turbo:before-visit`** — the first controller to need page-level (not element-scoped) listeners. This surfaced a real test-environment gap: `Application.stop()` only stops Stimulus's internal observers, it does **not** synchronously call `disconnect()` on mounted controllers — `disconnect()` only fires when the MutationObserver actually notices the controller's element leave the DOM. Every other test file's `afterEach` pattern (`application.stop()` then `document.body.innerHTML = ""`) accidentally stops the observer _before_ the element removal it would have reacted to, so `disconnect()` silently never runs in tests — invisible for every prior controller since their side effects were scoped to elements that vanish anyway, but it leaked this controller's `window`/`document` listeners across tests (a `turbo:before-visit` confirm spy saw 4 stale calls from earlier tests). Fixed only in this controller's own test file: clear `innerHTML` and await a tick _before_ calling `application.stop()`, so the observer is still running when the element is removed.
- **`unsaved-changes`'s `_onSubmit` defers the dirty-clear check with `queueMicrotask`, not a plain synchronous check** — found via `/code-review` on PR #24, before any external review. The original code called `markClean()` unconditionally on `submit`, with no regard for whether another listener on the same event (e.g. `form-confirm`'s `intercept()`, sharing the same `<form>`) had called `preventDefault()` to defer the real submission behind a dialog. A synchronous `if (!event.defaultPrevented)` check would still be wrong depending on listener registration order — `event.defaultPrevented` only reflects calls made _before_ this listener runs in the same dispatch. Deferring the check to a microtask lets all of the current dispatch's synchronous listeners (registered before or after `unsaved-changes`'s) finish first, so `defaultPrevented` is final and order-independent by the time it's checked. Without this, combining `unsaved-changes` with `form-confirm` and clicking Cancel on form-confirm's dialog left the form silently unguarded — edited but not submitted, with `unsaved-changes` already believing it was clean.
- **`unsaved-changes`'s `turbo:before-visit` handler skips prompting once `event.defaultPrevented` is already true, but does not fully dedupe across multiple dirty instances** — Copilot caught (PR #24) that each controller instance independently calls `window.confirm()` on the same `turbo:before-visit` event with no coordination, so N dirty forms on one page produce N stacked prompts for a single navigation attempt. Deliberately took the lighter fix over a full per-visit dedup (which would need module-level shared state — a `Set` of dirty instances plus a single shared listener — a bigger architectural step than this repo's one-file-per-controller minimalism default): the `event.defaultPrevented` guard stops the most jarring case (a dialog reappearing after the user already declined and the visit is already cancelled) without introducing cross-instance shared state. Each _separate_ dirty form still independently prompts on its own merits if the user hasn't already declined.
- **`relative-time` uses the standard MDN time-ago division table, not a hand-rolled threshold ladder** — divides the duration by each unit's ratio to the next, stopping at the first unit where the remaining magnitude is under 1 of the next. The table itself is correct, but the first implementation paired it with a rounding bug (compared the unrounded duration against each threshold, then formatted the rounded value) that produced exactly the "59.6 seconds → 60 seconds ago" boundary artifact the table is supposed to avoid — see the `copilot-review-triage` entry above for the fix (round once per iteration, compare and format the same rounded value).
- **`relative-time` caches its source string once in `connect()`, not re-reading `datetime`/`textContent` on every `render()`** — required once `render()` itself started overwriting `textContent` with the formatted output; re-reading `textContent` on a later tick would parse the controller's own previous output instead of the original timestamp. See the `copilot-review-triage` entry above.
- **`relative-time`'s test file needed real timers specifically for the axe-core assertion** — `axe-core`'s async internals rely on real `setTimeout` scheduling to resolve; with `vi.useFakeTimers()` active (needed for every other test in the file to control "now" and the re-render interval), `axe.run()` hangs until the test times out. Fixed by calling `vi.useRealTimers()` right before the `getA11yViolations` call in the accessibility test only, and guarding the shared `afterEach`'s `vi.advanceTimersByTimeAsync` behind `vi.isFakeTimers()` so it doesn't throw once timers are already real. Worth remembering for any future controller whose tests mix fake timers with the axe a11y check.
- **`direct-upload-progress` deliberately does not name an action method `initialize`** — Stimulus controllers have a reserved `initialize()` lifecycle callback (called once, before `connect()`), and ActiveStorage's first direct-upload event is literally named `direct-upload:initialize`. Naming the action handler `initialize` to match would silently override Stimulus's own lifecycle hook instead of registering a normal action method. The handler is named `add` instead; the README/HTML examples still wire `direct-upload:initialize->direct-upload-progress#add`.

### Open PRs (pending merge)

- None currently.

### Next components (planned)

None currently — the project is effectively feature-complete for its original scope. `multi-step-form` was the last candidate on this list but is being shelved rather than built: re-checked 2026-06-26, no canonical reference implementation exists to defer to (stimulus-components.com has no wizard/stepper controller; published examples are bespoke blog/demo code coupled to Bootstrap or StimulusReflex's server-roundtrip model). That absence is itself the signal worth weighing — it means step/validation/progress needs differ enough per-app that everyone hand-rolls their own rather than reaching for a shared package, which cuts against the "copy-pasteable, broadly reusable" premise this whole project is built on, not just against this one component's effort estimate. Revisit only if a concrete shared shape emerges from real usage, rather than building speculatively. New candidates can be added here as they come up; the README's "Well-covered elsewhere" guide remains the place to point users toward mature third-party packages for anything outside this scope.

### Open questions

- None currently.
